<a id="<%= dom_id(list_item) %>" class="mix-collection clearfix" href="<%= admin_mix_collection_path list_item %>">
	<div class="pull-left">
		<h5><%= list_item.name %></h5>
	</div>
	<div>
		<p class="audio-processing pull-right">
			<%= list_item.mixes_count %> Mixes
		</p>
	</div>
</a>